---
import Layout from "../../layouts/Layout.astro";
import "../../styles/project-page.css";

export async function getStaticPaths() {
  const projectFiles = import.meta.glob("./markdown/*.md", { eager: true });
  return Object.values(projectFiles).map((project: any) => {
    const slug = project.file.split("/").pop()?.replace(".md", "");
    return { params: { slug }, props: { project } };
  });
}

const { project } = Astro.props;
const { Content } = project;
const {
  title,
  id,
  category,
  status,
  image,
  trl,
  testing_type,
  propulsion_system,
  payload,
} = project.frontmatter;
---

<Layout title={`${title} | Technical Report`}>
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/katex@0.16.8/dist/katex.min.css"
    crossorigin="anonymous"
  />

  <article class="project-page">
    <header class="project-hero">
      <div class="hero-bg">
        <img src={image} alt="" />
        <div class="hero-overlay"></div>
      </div>

      <div class="hero-content">
        <nav class="breadcrumb">
          <a href="/projects">/ &lt; Return To Projects</a>
        </nav>
        <div class="meta-badge">PROJECT DOCS</div>
        <h1 class="project-title">{title}</h1>
        <div class="project-meta-strip">
          <div class="meta-item">
            <span class="label">FILE ID</span>
            <span class="value">{id || "PRJ-UNK"}</span>
          </div>
          <div class="meta-item">
            <span class="label">CATEGORY</span>
            <span class="value">
              {Array.isArray(category) ? category.join(", ") : category}
            </span>
          </div>
          <div class="meta-item">
            <span class="label">STATUS</span>
            <span class={`value status-${status?.toLowerCase()}`}>
              {status || "OPERATIONAL"}
            </span>
          </div>
        </div>
      </div>
    </header>

    <div class="project-body-wrapper">
      <aside class="tactical-sidebar">
        <div class="sidebar-box glass">
          <h3>PROJECT INFO</h3>
          <ul class="info-list">
            <li>
              <span>TESTING TYPE:</span>
              <span class="info-value">{testing_type || "To Be Tested"}</span>
            </li>
            <li>
              <span>PROPULSION SYSTEM:</span>
              <span class="info-value">
                {propulsion_system || "No Propulsion System"}
              </span>
            </li>
            <li>
              <span>PAYLOAD:</span>
              <span class="info-value">{payload || "No Payload"}</span>
            </li>
          </ul>
        </div>

        <div class="sidebar-box glass">
          <h3>TECHNOLOGY READINESS LEVEL</h3>
          <div class="stat-row">
            <span class="stat-label">{trl ? `${trl}` : "N/A"}</span>
          </div>
        </div>
      </aside>

      <section class="markdown-content">
        <Content />
      </section>
    </div>
  </article>
</Layout>

<style is:global>
  /* GFM Table Styling */
  .markdown-content table {
    display: block;
    width: 100%;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    margin: 2rem 0;
    border-collapse: collapse;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
  .markdown-content th,
  .markdown-content td {
    min-width: 120px;
    padding: 12px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    text-align: left;
  }
  .markdown-content th {
    background: rgba(255, 255, 255, 0.07);
  }

  /* GFM Task List Styling */
  .markdown-content ul li input[type="checkbox"] {
    margin-right: 10px;
    accent-color: #00ffaa;
  }

  /* Math Centering */
  .katex-display {
    margin: 1.5rem 0;
    overflow-x: auto;
    overflow-y: hidden;
  }
</style>
